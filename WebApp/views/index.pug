extends layout

block content
  if(items)
    for item in items
      div(class="item")
        h3=item.title
        h4="$" + item.unitPrice
        h5=item.description
        button="Add to cart"
  else
    h2="There are currently no items"