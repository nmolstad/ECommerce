extends layout

block content
  if(items && items.length > 0)
    for item in items
      div(class="item")
        h3=item.title
        h4="$" + item.unitPrice
        h5=item.description
        button(onclick="AddItemToCart()")="Add to cart"
  else
    h2="There are currently no items"